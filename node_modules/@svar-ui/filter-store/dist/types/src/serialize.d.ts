import type { IFilterSet, IFilter } from "./types";
export interface SerializeResult {
    query: string;
    warnings: string[];
}
export declare function serialize(filter: IFilterSet | IFilter): SerializeResult;
