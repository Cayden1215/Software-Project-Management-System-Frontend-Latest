import type { Token } from "./types";
declare const TEXT_OPERATORS: readonly ["contains", "starts", "ends"];
declare const LOGICAL_OPERATORS: readonly ["and", "or"];
export { TEXT_OPERATORS, LOGICAL_OPERATORS };
export declare class Tokenizer {
    private input;
    private pos;
    private tokens;
    private offset;
    constructor(input: string, offset?: number);
    private peek;
    private advance;
    private isEnd;
    private skipWhitespace;
    private readQuotedString;
    private readWord;
    tokenize(): Token[];
}
export declare function tokenize(text: string): Token[];
