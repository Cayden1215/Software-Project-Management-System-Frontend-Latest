import type { IFilterSet, IFilter, IField, AnyData, IDataHash } from "../types";
import type { Token, HighlightToken, ValidationError, ParseResult, ParseOptions } from "./types";
export declare class Parser {
    private tokens;
    private pos;
    private fields;
    private options;
    private fieldMap;
    private highlightTokens;
    private validationErrors;
    constructor(tokens: Token[], fields: IField[], options: IDataHash<AnyData[]>);
    private peek;
    private advance;
    private isEnd;
    private expect;
    private emit;
    getHighlightTokens(): HighlightToken[];
    getValidationErrors(): ValidationError[];
    private getFieldType;
    private coerceValue;
    parse(): IFilterSet | IFilter;
    private parseOrExpression;
    private parseAndExpression;
    private isStartOfFilterRule;
    private combineWithGlue;
    private parsePrimaryExpression;
    private parseGroupedExpression;
    private parseQuickSearch;
    private parseFieldRule;
    private parseValueList;
    private parseValue;
    private parseSimpleValue;
    private emitValueTokens;
    private comparisonToFilter;
    private textOpToFilter;
    private buildFilterFromValues;
    private buildSingleRule;
    private validateAndCoerce;
}
export declare function parse(text: string, fields?: IField[], options?: IDataHash<AnyData[]>, parseOptions?: ParseOptions): ParseResult;
export declare function parseSet(text: string, fields?: IField[], options?: IDataHash<AnyData[]>, parseOptions?: ParseOptions): ParseResult;
