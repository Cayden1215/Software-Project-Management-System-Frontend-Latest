import { TFilterHandler, TFilterType, IFilterValues, IFilterColumn } from "./types";
export declare const filtersHandlers: {
    [key: string]: TFilterHandler;
};
export declare function getFilterHandler(type: TFilterType): TFilterHandler;
export declare function createFilter(filterValues: IFilterValues, columns: IFilterColumn[]): (obj: any) => boolean;
